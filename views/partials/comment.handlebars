<div class="comment row">
	<hr style="margin-bottom: 2px; margin-top:2px;"/>
	<div class="col-md-1">
		<img src="{{user.profile_image}}" class="prof_pic-small" />
	</div>
	<div class="col-md-10">
		<a href="user/{{user._id}}">{{user.username}}</a> {{text}}
		<br />
		X hours ago
	</div>
</div>